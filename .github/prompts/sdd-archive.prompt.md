---
description: "Archive a verified SDD change"
agent: "agent"
argument-hint: "Change name"
---
Archive the completed SDD change with the dedicated `sdd-archive` agent. Proceed only after verification has passed and required delivery gates are satisfied. Return the archive result and any remaining risks.

Change: ${input:change}
