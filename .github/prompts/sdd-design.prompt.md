---
description: "Create the technical SDD design"
agent: "agent"
argument-hint: "Change name"
---
Create the technical design for the requested SDD change with the dedicated `sdd-design` agent. Read and validate the proposal and specification first, enforce SDD gates, and persist the design. Do not implement code.

Change: ${input:change}
