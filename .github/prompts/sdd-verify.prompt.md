---
description: "Verify an implemented SDD change"
agent: "agent"
argument-hint: "Change name"
---
Verify the implemented SDD change with the dedicated `sdd-verify` agent. Compare implementation against the proposal, specification, design, and tasks, run the applicable checks, and return the structured verification result.

Change: ${input:change}
