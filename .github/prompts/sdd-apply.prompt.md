---
description: "Implement ready SDD tasks"
agent: "agent"
argument-hint: "Change name"
---
Implement the ready SDD tasks with the dedicated `sdd-apply` agent. Confirm that proposal, specification, design, and tasks exist, enforce the native attempt authority and review workload guard, then implement and report completed or blocked tasks.

Change: ${input:change}
