---
description: "Write SDD requirements and scenarios"
agent: "agent"
argument-hint: "Change name"
---
Write the SDD specification for the requested change with the dedicated `sdd-spec` agent. Enforce the SDD init guard and session preflight, read the proposal, and persist requirements and scenarios in the active artifact store.

Change: ${input:change}
