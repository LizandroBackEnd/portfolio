---
description: "Create an SDD change proposal"
agent: "agent"
argument-hint: "Change name and intent"
---
Create an SDD proposal with the dedicated `sdd-propose` agent. Enforce SDD Session Preflight, retrieve the required exploration or change context, and persist the proposal in the active artifact store. Do not implement code.

Change request: ${input:change}
