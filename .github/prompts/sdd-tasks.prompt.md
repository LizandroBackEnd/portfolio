---
description: "Break an SDD change into tasks"
agent: "agent"
argument-hint: "Change name"
---
Break the requested SDD change into ordered implementation tasks with the dedicated `sdd-tasks` agent. Read the specification and design, enforce SDD gates, calculate the review workload forecast, and persist the task checklist.

Change: ${input:change}
